/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // Các cấu hình khác của bạn có thể nằm ở đây
    images: {
      unoptimized: true,
    },
  };
  
export default nextConfig;
  
